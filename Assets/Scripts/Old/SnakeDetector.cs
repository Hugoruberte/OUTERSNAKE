using UnityEngine;
using System.Collections;

public class SnakeDetector : MonoBehaviour
{
	/*private SnakeControllerV3 snakeScript;
	private Transform zoneTransform = null;

	void Awake()
	{
		snakeScript = GameObject.FindWithTag("Player").GetComponent<SnakeControllerV3>();
	}

	void OnTriggerEnter(Collider other)
	{
		if(other.CompareTag("ZoneSnake") && zoneTransform != other.transform && snakeScript.State == SnakeState.Run)
		{
			zoneTransform = other.transform;
			snakeScript.switchNotch = 1;
		}
	}*/
}